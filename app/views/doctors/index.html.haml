%header.site-header
  %p.site-title= t(:app_purpose)
  = render "doctors/doctor_search_form"
%main.main-content
  - if @q
    %h1.results-summary
      = t("search_results_summary", count: @doctors.length, query: @q)
  %ul.doctors-list
    = render @doctors
